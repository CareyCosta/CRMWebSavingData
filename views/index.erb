<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/styles.css">
    <title></title>
  </head>
  <body>

    <header>
    <h1><%= @crm_app_name%></h1>
    <ul class="nav">
      <li>Home</li>
      <li>Add a contact</li>
      <li>About</li>
    </ul>
    </header>

    <ul class="all_contacts">
      <p>There are <%= Contact.all.count %> contacts</p>
      <% Contact.all.each do |contact| %>
      <li><a href="/contacts/<%=contact.id%>"><%= contact.first_name %> <%= contact.last_name %> (<%= contact.email %>)</a>
          <footer class="contact_foot"><a href="/contacts/<%=contact.id%>/edit">Edit</a> | <a href="/contacts/<%=contact.id%>">Delete</a>
            </li>
      <% end %>
      </ul>
    <footer class="bitmaker">Bitmaker 2016</footer>
  </body>
</html>
