<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/styles.css">
    <title></title>
  </head>
  <body>

    <header>
    <h1><%= @crm_app_name%></h1>
    <ul class="nav">
      <li>Home</li>
      <li>Add a contact</li>
      <li>About</li>
    </ul>
    </header>

    <ul class="all_contacts">
      <p>There are <%= Contact.all.count %> contacts</p>
      <% Contact.all.each do |contact| %>
      <li><%= contact.first_name%> <%= contact.last_name %> (<%= contact.email %>)
          <footer class="contact_foot"><a href="new_contact"> Edit </a>|<a href="contacts"> Delete</a></footer>        </li>
      <% end %>
      </ul>
    <footer class="bitmaker">Bitmaker 2016</footer>
  </body>
</html>
